pipeline {
    agent any

    stages {
        stage('Hello') 
        {
            steps 
            {
                echo 'this is build '
                echo 'this is build'
            }
        }
    }
}
